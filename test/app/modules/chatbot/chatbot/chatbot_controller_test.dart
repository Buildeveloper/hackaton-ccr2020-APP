import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:ace/app/modules/chatbot/chatbot_controller.dart';
import 'package:ace/app/modules/chatbot/chatbot_module.dart';

void main() {
  initModule(ChatbotModule());
  // ChatbotController chatbot;
  //
  setUp(() {
    //     chatbot = ChatbotModule.to.get<ChatbotController>();
  });

  group('ChatbotController Test', () {
    //   test("First Test", () {
    //     expect(chatbot, isInstanceOf<ChatbotController>());
    //   });

    //   test("Set Value", () {
    //     expect(chatbot.value, equals(0));
    //     chatbot.increment();
    //     expect(chatbot.value, equals(1));
    //   });
  });
}
